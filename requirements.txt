# Dépendances Python pour le générateur de site statique (Excel -> HTML)
# (Les modules de la bibliothèque standard comme argparse/pathlib/json/ftplib/tkinter ne figurent pas ici.)

pandas>=2.1,<3

# Lecture Excel (.xlsx et .xls)
openpyxl>=3.1,<4
xlrd>=2.0,<3

# Markdown (build_site.py : import markdown as md)
Markdown>=3.6,<4

# Validation ONIX (onix_validator.py : import onixcheck)
onixcheck>=0.9.11,<0.10
